added frontend as bastion in the ssh/config file
added hosts backend and postgress.

ssh backend from local

ssh postgress form local


for app connectivity testing
make docker offline directory 
curl -fsSL https://download.docker.com/linux/static/stable/x86_64/docker-24.0.7.tgz -o docker.tgz
tar xzvf docker.tgz

docker pull redis
save redis.tar

scp docker and redis to bastion
scp -i /home/squreshi/Desktop/Project1/ironhack-project-1/terraform/sana_project1.pem -r docker redis.tar bastion:/home/ubuntu/
(took a loooooooooooooooooong time)

scp from bastion to backenddoesnt work because ssh config is only local. scp to backend from local as well
scp -r -F ~/.ssh/config docker redis.tar backend:/home/ubuntu/
✅ This does:

Uses your bastion host (54.91.252.62) as a jump

Copies docker/ and redis.tar to the backend (10.0.2.69)

Authenticates using your PEM file




install docker in backend and run redis

after redis is running, go back to frontend and run
telnet 10.0.2.69 6379
# or
nc -zv 10.0.2.69 6379